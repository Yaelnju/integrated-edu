# 作业3 报告素材 — 学院 A + XML 集成（与 B/C 联调版）

## 1. 总体架构

```mermaid
flowchart TB
    subgraph colleges [三院教务系统]
        A["学院 A\nSQL Server\n9002 / 9102"]
        B["学院 B\nOracle\n9001 / 9101"]
        C["学院 C\nMySQL\n9000 / 9100"]
    end
    IS["XML 集成服务器\n9200\n(DataIntegration-collegeA)"]
    A <-->|GET_* / ENROLL| IS
    B <-->|GET_* / ENROLL| IS
    C <-->|GET_* / GUI PICK| IS
```

## 2. 跨院选课

```mermaid
sequenceDiagram
    participant GUI as A院 GUI
    participant A9002 as A 业务 9002
    participant IS as 集成 9200
    participant B9101 as B XML 9101

    GUI->>A9002: CROSS_ENROLL|A20240001|B0001|B
    A9002->>IS: 转发 CROSS_ENROLL
    IS->>B9101: GET_COURSES 校验 SHARED=1
    IS->>A9101: ENROLL 写源院选课
    IS->>B9101: ENROLL 写回外院学生
    IS-->>GUI: OK + 结果 XML
```

## 3. 集成退选

```mermaid
flowchart TD
    S[学生点击退课] --> L[本院 DROP 删除 SC]
    L --> I[发送 INTEGRATED_DROP 到 9200]
    I --> O[识别课程所属院 B/C/A]
    O --> R[远程 DROP 删除开课院记录]
    R --> OK[返回成功]
```

## 4. 全院统计

```mermaid
flowchart LR
    U[GUI 集成统计] --> IS[STATS_ALL]
    IS --> A[GET_STUDENTS/COURSES/CHOICES @9102]
    IS --> B[GET_* @9101]
    IS --> C[GET_* @9100]
    A --> SUM[汇总条数]
    B --> SUM
    C --> SUM
    SUM --> U
```

## 5. 技术说明（报告正文可用）

1. **XML 交换**：各院 `XmlTcpServer` 导出本地 schema 的 XML，经 XSD 校验后供集成服务器使用。
2. **XSL 映射**：`studentA_to_unified.xsl` 等将异构字段转为 `formatStudent.xsd` 统一结构。
3. **异构持久化**：学院 A 使用 SQL Server 表 `Student/Course/SC/Account`，字段命名与 Oracle/MySQL 院不同。
4. **写回机制**：跨院选课后集成服务器对**源院**与**开课院**分别调用 `ENROLL`（C 院使用 GUI `PICK`）。
